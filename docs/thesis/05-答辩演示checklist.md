# 答辩演示 Checklist（对应 grilling Q4 确认 + 实测补充）

> 用途：答辩物料，非论文内容。四件套已确认在位，本文件把它们钉成可勾选的执行单。

## T-1 天（答辩前一天晚）

- [ ] **全管道重采**：P1 采集 → P2 处理 → P3 聚合 → P4 合并摘要全量重跑，保证库内数据新鲜（时间词查询依赖当日数据；冻结旧库会让一切「最近/今天」查询返回空）
- [ ] **DeepSeek 限额检查**：余额/速率限额足够撑过演示 + 评委自由提问环节
- [ ] **录屏 PlanB**：完整录制核心演示路径（检索→引用→溯源→简报），现场服务挂掉时切录屏
- [ ] 重采后跑一遍白名单问题逐条确认（尤其拒答题仍正确拒答）

## 演示问题白名单（只问验证过的）

| # | 问题 | 预期 | 已验证 |
|---|---|---|---|
| 1 | 政务数据共享条例的主要内容是什么 | 检索命中事件#51 等，答案带有效 [事件#X] 引用 | ✅ citations_valid=true |
| 2 | 任一白名单外领域提问（如无关话题） | 正确拒答「信息不足」 | ✅ refusal 16/16 |
| 3 | （重采后）今日/昨日新闻简报 | /digest 页非空，跨源事件靠前 | ⚠️ 重采后补验 |

## 演示动线

1. 事件流页：指 **#132 跨源聚合案例**（中国新闻网 × 澎湃新闻 × 澎湃热榜，多源度=3 排序第一）——这是「多源聚合」主张的活证据
2. 提问页：白名单问题 → 展示答案内嵌引用 → 点引用跳转事件详情 → 事件详情跳原文链接（溯源闭环）
3. 每日简报页：结构化清单、逐字段可追溯
4. （被追问指标时）分层评测表 + 消融表

## 应急预案

- 后端无响应 → 切录屏
- LLM 超时/限额 → 重试一次，仍失败切录屏
- 评委问到没准备的数字 → 报分层口径（全量 0.452 主表 + 检索题 0.616 / 拒答 **1.000**=16/16），不现算；若被追问 0.958：那是含 2 道时间词误拒的全量行口径（46/48），根因是冻结快照数据过期，见 02 文件口径说明

## 口径备忘（防口误）

- 对外排序与展示口径是**多源度**（不同媒体来源数），不是报道篇数
- recall@5 全量 0.452 是"检索+生成"整体口径，检索题单独 0.616
- 0.90 阈值：可迁移的是标定流程不是数字
